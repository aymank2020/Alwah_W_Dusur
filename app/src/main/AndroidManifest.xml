<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aymanx.ai.alwahwdusur">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_alwah_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_alwah_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.RegistrationActivity"></activity>
        <activity
            android:name=".ui.activity.DetailsActivity"
            android:label="@string/title_activity_details"
            android:parentActivityName=".ui.activity.CategoryActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.aymanx.ai.alwahwdusur.ui.activity.WelcomeActivity" />
        </activity>
        <activity android:name=".ui.activity.CategoryActivity" />
        <activity
            android:name=".ui.activity.WelcomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.activity.ContactUsActivity" />
        <activity
            android:name=".ui.activity.SplashActivity"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>